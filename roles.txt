name level_accses

Администратор 10
Модератор 5
Журналист 3
Пользователь 1

      # && psql -U postgres -d kokoc CREATE EXTENSION pg_trgm
      # && pg_restore -U postgres -d kokoc /backup/kokoc_db.backup


      command:
      - "postgres"
      - bash -c 'psql -U postgres -d kokoc -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;"';
      - bash -c 'pg_dump -U postgres -d kokoc -s | grep -q "CREATE TABLE" || pg_restore -U postgres -d kokoc /backup/kokoc_db.backup'
